---
name: tests
run-name: Run tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }} ${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  test:
    name: Busted tests (${{ matrix.lua }})
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        lua:
          - "5.1"
          - "luajit-2.1"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Lua
        uses: leafo/gh-actions-lua@v12
        with:
          luaVersion: ${{ matrix.lua }}

      - name: Install LuaRocks
        uses: leafo/gh-actions-luarocks@v6
        with:
          luarocksVersion: "3.12.2"

      - name: Install dependencies
        run: |
          luarocks install busted
          luarocks install luacov
          luarocks install luacov-coveralls

      - name: Build native timer
        run: make clock

      - name: Run tests
        run: busted --verbose --coverage

      - name: Generate coverage report
        run: luacov

      - name: Print coverage summary
        run: tail -n 15 luacov.report.out

      - name: Submit to Coveralls
        if: success() && env.COVERALLS_REPO_TOKEN != ''
        run: luacov-coveralls --service-name github-actions --verbose
        env:
          COVERALLS_REPO_TOKEN: ${{ github.token }}

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: luacov.report.out
